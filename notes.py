#Dictionaries

.pop() # removes key/value pair
.keys() # retrieves list of keys
.items() # retrieves key/value pairs
    for key, value in dict.items():
        print("Input key:" + key + "and Value: " + value)