import commands:
import React from 'react';
import ReactDOM from 'react-dom';

import { ComponentName } from './Component'

stateful component:
class Parent extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      name: 'Frarthur'
    }
  }
  render() {
    return <div></div>;
  }{this}
}

stateless component:
export class Child extends React.Component {
  render() {
    return <h1>Hey, my name is {this.props.name}!</h1>
  }
}

ReactDOM.render(<Parent />, document.getElementById('app'))

map method to render each element:
{this.props.businesses.map(business => {
          return <Business key={business.id} business={business} />;
        })}

callback function:
variable => {}

find if state value exists:
if (this.state.playlistTracks.find(savedTrack => savedTrack.id === track.id))

props vs state:
state is a private object to a specific component
props object is used to share state values with other objects
Props are read only

.map:
this.state.playlistTracks.map(item => item.id)

VS Code Shortcuts
Windows + Shift + L - selects multiple instances

Question Log:
1. What does <link rel="stylesheet" do?
2. 

